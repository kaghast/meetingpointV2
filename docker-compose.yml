services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: interactive-meeting-poll:latest
    container_name: meeting-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./data:/app/data
